# Docker integration — lets Homepage detect container status.
# Uses the Docker socket proxy (dockerproxy container) instead of the raw
# socket for security — only read-only API endpoints are exposed.

local:
  host: dockerproxy
  port: 2375
