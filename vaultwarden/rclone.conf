# Rclone local remote — stores backups to a Docker volume instead of cloud.
# Used by the vaultwarden-backup container (stacks/vaultwarden-backup.yaml).
# To add cloud backup later, run: docker exec -it vaultwarden-backup rclone config
[VaultwardenBackup]
type = local
